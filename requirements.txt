# LLM / OCR
openai>=1.0
mistralai>=0.3  # legacy OCR fallback — set OCR_ENGINE=mistral to use

# Core pipeline
rapidfuzz>=3.0
pypdf>=4.0
python-dotenv>=1.0  # load .env.gpustack for GPUStack config

# Optional: local OCR (set OCR_ENGINE=easyocr to use)

